name: lineosaurus
description: let's count
author: Nicholas Valentinus
branding:
  icon: align-justify
  color: gray-dark

inputs:
  git-name:
    description: Git name
  git-email:
    description: Git email
  show-credit:
    description: show the credit
    required: false
    default: true
  only_type:
    description: "['.py', '.js']"
    required: false
  ignore_type:
    description: "['.py', '.js']"
    required: false

runs:
  using: 'composite'
  steps:      
    - shell: bash
      run: |
        echo "GITHUB_ACTOR: $GITHUB_ACTOR"
        echo "INPUT_ONLY_TYPE: $INPUT_ONLY_TYPE"
        # x=$(gh repo list nvfp --visibility public --json url)
        # y=$(python $GITHUB_ACTION_PATH/src get-num-repos $x)
        # URLS=$(python $GITHUB_ACTION_PATH/src get-clone-urls $x)
        # echo "x: '$x'"
        # echo "y: '$y'"
        # echo "URLS: '$URLS'"

        # mkdir $GITHUB_WORKSPACE/../workspace
        # cd $GITHUB_WORKSPACE/../workspace

        # echo "::group::ls $(pwd)"
        # ls
        # echo "::endgroup::"

        # for url in $URLS; do
        #   git clone $url
        # done

        # echo "::group::ls $(pwd)"
        # ls
        # echo "::endgroup::"
    # - uses: actions/checkout@v3
    # - shell: bash
    #   run: ls
    # - uses: scapeville/action-git-add-all-then-commit-then-push@v1
    #   with:
    #     name: ${{ env.git-name }}
    #     email: ${{ env.git-email }}
    #     msg: "Updated on $(date +'%Y %b %e, %l:%M %p')"